- via ssh start App in background / start App via systemd
- App creates and starts MasterModule and creates and starts SocketServer
- MasterModule creates and starts PositionPushingThreadPoolExecutor
- from then on MasterModule or another appropriate Module can submit the current position to PositionPushingThreadPoolExecutor to be sent via network